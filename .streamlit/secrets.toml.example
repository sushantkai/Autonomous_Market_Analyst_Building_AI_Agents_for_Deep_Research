# Streamlit Cloud secrets template — for reference only, don't commit real secrets.
# When deploying: paste these (with real values) into your app's Settings -> Secrets.

GOOGLE_API_KEY = "your_google_gemini_api_key_here"
SERPER_API_KEY = "your_serper_dev_api_key_here"